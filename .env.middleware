# Middleware Service Configuration
# Deploy this file to: middleware VM (34.128.100.247)
# Location on VM: ~/smartcom-tech-test/services/middleware/.env

PORT=8080

# Using internal IP for better performance (same zone)
EXTERNAL_ENDPOINT_URL=http://10.184.0.4:8081/external/alerts

QUEUE_SIZE=1000
WORKER_COUNT=10
HTTP_TIMEOUT=3s
MAX_RETRIES=3
BASE_DELAY=500ms
