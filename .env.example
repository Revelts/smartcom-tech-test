# Middleware Service Configuration (for VM: middleware - 34.128.100.247)
PORT=8080

# External Endpoint URL - Use internal IP for better performance (both VMs in same zone)
EXTERNAL_ENDPOINT_URL=http://10.184.0.4:8081/external/alerts

# Alternative: Use external IP if accessing from different network
# EXTERNAL_ENDPOINT_URL=http://34.50.103.62:8081/external/alerts

# Queue and Worker Configuration
QUEUE_SIZE=1000
WORKER_COUNT=10

# HTTP Client Configuration
HTTP_TIMEOUT=3s
MAX_RETRIES=3
BASE_DELAY=500ms

# ========================================
# VM Information (for reference)
# ========================================
# Middleware VM:
#   - Name: middleware
#   - Zone: asia-southeast2-a (Jakarta)
#   - Internal IP: 10.184.0.3
#   - External IP: 34.128.100.247
#   - Port: 8080
#
# External Endpoint VM:
#   - Name: external-ep
#   - Zone: asia-southeast2-a (Jakarta)
#   - Internal IP: 10.184.0.4
#   - External IP: 34.50.103.62
#   - Port: 8081
#
# ========================================
# Usage
# ========================================
# For Middleware VM: Copy this file to .env
#   cp .env.example .env
#
# For External Endpoint VM: Only PORT is needed
#   echo "PORT=8081" > .env
#
# Test Middleware from outside:
#   curl http://34.128.100.247:8080/integrations/events
#
# Test External Endpoint from outside:
#   curl http://34.50.103.62:8081/health
